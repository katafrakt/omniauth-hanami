require "spec_helper"

describe Omniauth::Hanami do
  it "has a version number" do
    expect(Omniauth::Hanami::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
